{
  "name": "ts",
  "version": "1.0.0",
  "description": "TypeScript templ example.",
  "scripts": {
    "build": "esbuild --minify --format=esm --sourcemap --outdir=../assets/js ./src/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.21.3",
    "jspm": "^3.2.0",
    "lightweight-charts": "^4.1.7",
    "typescript": "^5.4.5"
  }
}
