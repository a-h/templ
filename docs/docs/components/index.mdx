import DocCardList from '@theme/DocCardList';

# Components

Components are the basic building block of templ.

Components can be written in:

* templ syntax, which is converted into Go code by using the `templ generate` command.
* Go code, which is suitable if you need a high level of control of the output.

<DocCardList />
