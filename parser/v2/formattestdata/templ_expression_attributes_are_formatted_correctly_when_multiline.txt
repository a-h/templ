-- in --
package main

import "github.com/a-h/templ"

templ x(id string, class string) {
<button
id={id}
name={
      "name"
  }
class={ 
      "blue",
    class,
		map[string]bool{
		"a": true,
		},
}
></button>
}
-- out --
package main

import "github.com/a-h/templ"

templ x(id string, class string) {
	<button
		id={ id }
		name={ "name" }
		class={
			"blue",
			class,
			map[string]bool{
				"a": true,
			},
		}
	></button>
}
