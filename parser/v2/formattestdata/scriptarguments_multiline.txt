-- in --
package p

import "github.com/a-h/templ"

script Style(
a string,
b string,
c string,
) {
	console.log(a, b, c);
}
-- out --
package p

import "github.com/a-h/templ"

script Style(
	a string,
	b string,
	c string,
) {
	console.log(a, b, c);
}
