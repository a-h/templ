-- in --
package p

import "github.com/a-h/templ"

templ List(
list1 list[item],
list2 list[item],
list3 list[item],
) {
	<div></div>
}
-- out --
package p

import "github.com/a-h/templ"

templ List(
	list1 list[item],
	list2 list[item],
	list3 list[item],
) {
	<div></div>
}
